# Python Notes

-----

## Version

Requires Python 3.10+. Currently using Python 3.13.

-----

## Virtual Environment

To quickly create a virtual environment, navigate to the directory of your repository and run:

```sh
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install pylint autopep8
python -m pip install ansible ansible-lint
python -m pip install asyncssh
python -m pip freeze -> requirements.txt
```

-----

## Additional Notes

- TBD.
